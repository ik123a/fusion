apiVersion: apps/v1
kind: Deployment
metadata:
  name: fusion-redis
  namespace: fusion
  labels:
    app: fusion-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fusion-redis
  template:
    metadata:
      labels:
        app: fusion-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: fusion-redis
  namespace: fusion
spec:
  selector:
    app: fusion-redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP
